#include <vector>
#include <cmath>

void print(std::vector <int> arr);
int length(int num);
int extract_digit(int num);
std::vector<int> square_vector(std::vector<int> arr);
std::vector<int> extract_digits(std::vector<int> arr);
int square_digit(int num);
